### Fixed

- Request the missing block on demand when a Gloas data column sidecar or PTC payload attestation arrives before its block, and downscore a forwarder at most once per orphaned root instead of once per column.
