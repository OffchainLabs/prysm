### Added

- Added a new `--network` flag to specify which network to connect to (mainnet, sepolia, holesky).
- Added a warning that old network flags (`--mainnet`, `--sepolia`, `--holesky`) will be deprecated in Prysm v7.0.