### Fixed

- Serve available historical Gloas payload envelopes by root, and recover from peers that omit pre-finalization envelopes using a bounded range fallback restricted to slots before the peer's finalized epoch.
