### Added

- Add `payload_attestation_inserts_total` counter, labelled by `payload_present` and `blob_data_available`, to expose the distribution of PTC member claims observed by the payload-attestation pool.
