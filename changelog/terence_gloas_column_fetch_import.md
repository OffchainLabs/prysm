### Fixed

- Don't block Gloas block import on missing data column sidecars; DA is checked on the payload envelope.
- Don't require data columns for a Gloas checkpoint-sync origin block whose payload was never revealed.
