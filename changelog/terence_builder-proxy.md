### Added

- Per-builder `builders` entries in proposer settings with `url` as the HTTP dial target and opaque `auth_data` signed into request auths (defaulting to the URL bytes), plus per-builder pubkey payment binding, max_execution_payment, min_bid, and builder_boost_factor; `relays` remains a deprecated alias.
