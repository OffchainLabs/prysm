### Added

- Add `gloas_payload_reveal_outcome_total{outcome}` metric recording the per-slot execution payload reveal outcome (`on_time`, `late`, or `withheld`), giving operators the core ePBS signal for builder payload withholding.
