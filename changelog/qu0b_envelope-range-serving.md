### Fixed

- Respond resource unavailable instead of a server error when the execution client cannot reconstruct payloads for a Gloas execution payload envelopes by-range request, and keep the contiguous prefix of a by-range response on a mid-response parent hash discontinuity instead of downscoring the serving peer.
