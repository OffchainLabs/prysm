# blst

This directory contains build support files for [blst](https://github.com/supranational/blst),
a BLS12-381 signature library used in Prysm.

The blst library's Go bindings are managed as a standard Go module dependency via `go.mod`.
